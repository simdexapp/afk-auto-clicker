# Python
__pycache__/
*.pyc

# PyInstaller build output
build/
dist/
*.spec

# Release artifacts (installer + zip are attached to GitHub Releases instead)
release/

# Local user settings
*.afk_autoclicker*.json

# legacy CustomTkinter prototype (superseded by app.py + engine.py)
clicker.py

# local signing config (never commit the real one; secret-free but machine-specific)
*.local.ps1
